{
  "name": "gitlens",
  "private": true,
  "description": "Visual narrative of code history — interactive git timeline with AI-powered semantic analysis",
  "scripts": {
    "dev": "concurrently \"pnpm --filter @gitlens/server dev\" \"pnpm --filter @gitlens/web dev\"",
    "dev:server": "pnpm --filter @gitlens/server dev",
    "dev:web": "pnpm --filter @gitlens/web dev",
    "build": "pnpm --filter @gitlens/engine build && pnpm --filter @gitlens/server build && pnpm --filter @gitlens/web build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
